#' Read CTF data
#' 
#' Read external CTF data into the corresponding R object.
#' 
#' @param dir path to a directory containing CTF data
#' @param columns path to a directory containing CTF data
#' @param ... further arguments to \code{scan}
#' @return data frame containing of ctf data
#' @export
read_ctf = function(dir, columns = NULL, ...) 
    NULL
